server:
  port: 8081
ani:
  swagger:
    title: 用户服务接口文档
    package: com.anihub.user.controller
  db:
    database: ani-user
  jwt:
    location: classpath:anihub.jks
    alias: anihub
    password: bianxin123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
      - /hi
